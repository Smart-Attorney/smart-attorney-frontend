class RouteHandler {
	constructor() {}

	public handlePost(path: string, data?: unknown) {
		switch (path) {
			case "value":
				data;
				break;

			default:
				break;
		}
	}

	public handleGet(path: string, data?: unknown) {
		switch (path) {
			case "value":
				data;
				break;

			default:
				break;
		}
	}

	public handlePut(path: string, data?: unknown) {
		switch (path) {
			case "value":
				data;
				break;

			default:
				break;
		}
	}

	public handleDelete(path: string, data?: unknown) {
		switch (path) {
			case "value":
				data;
				break;

			default:
				break;
		}
	}
}

export default RouteHandler;
